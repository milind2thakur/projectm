"""Placeholder text-to-speech module for future voice output."""

from __future__ import annotations


def speak(text: str) -> str:
    """Placeholder TTS call that returns a status message."""
    return f"[tts-placeholder] speaking: {text}"
