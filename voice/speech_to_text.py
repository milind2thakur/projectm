"""Placeholder speech-to-text module for future voice integration."""

from __future__ import annotations


def transcribe(audio_source: str) -> str:
    """Return placeholder text from an audio source path/device descriptor."""
    return f"[stt-placeholder] transcribed input from: {audio_source}"
